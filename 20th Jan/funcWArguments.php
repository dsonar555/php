<?php

/* $var1=5;
$var2=10;

function addNum($n1, $n2)
{
	echo '<br>n1='.$n1.' & n2='.$n2;
	echo '<br>The addition is : ';
	echo $n1 + $n2;
}

addNum($var1,$var2);
addNum($var2,$var1);
 */

/* function displayDate($date, $month, $year)
{
	echo "$date - $month - $year";
}

$now=new DateTime('now');

displayDate($now->format('d'), $now->format('F'), $now->format('Y'));
 */
/* 
function displayDate($now)
{
	echo $now->format('d')." - ".$now->format('F')." - ".$now->format('Y');
} 
// $now=new DateTime('now');
// displayDate($now);
// displayDate( new DateTime('now') );
 */

// function concatenate( &$str )
// {
// 	$str .= " and something extra is added.";
// }

// $str="this is ecxample of string";
// echo $str."<Br>";
// concatenate($str);
// echo $str;



?>